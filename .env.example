# Copy this file to `.env` and fill in the real value.
# `.env` is gitignored; `.env.example` is safe to commit (no secrets).
#
#   cp .env.example .env
#   then edit .env and paste your key
#
# The generation pipeline reads exactly this variable name:
ANTHROPIC_API_KEY=sk-ant-...
